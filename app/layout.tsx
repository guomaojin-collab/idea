import "./globals.css";
export const metadata={title:"idea — catch it before it's gone",description:"A calm, ADHD-friendly place to catch thoughts before they disappear."};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}